cmake_minimum_required(VERSION 3.20)
project(cv_final)
set(CMAKE_CXX_STANDARD 17)
if(WIN32)
    add_definitions(-DNOMINMAX)
    add_definitions(-D_CRT_SECURE_NO_WARNINGS )
endif()
add_subdirectory(cv_final)